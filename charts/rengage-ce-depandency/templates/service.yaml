apiVersion: v1
kind: Service
metadata:
  name: {{ include "rengage-ce-depandency.fullname" . }}
  labels:
    {{- include "rengage-ce-depandency.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rengage-ce-depandency.selectorLabels" . | nindent 4 }}
