apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rengage-ce-depandency.fullname" . }}-test-connection"
  labels:
    {{- include "rengage-ce-depandency.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rengage-ce-depandency.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
