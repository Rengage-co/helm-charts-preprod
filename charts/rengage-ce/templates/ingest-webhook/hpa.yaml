{{- if .Values.ingest-webhook.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ printf "%s-ingest-webhook" (include "rengage-ce.fullname" .) }}
  labels:
    {{- include "rengage-ce.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ printf "%s-ingest-webhook" (include "rengage-ce.fullname" .) }}
  minReplicas: {{ .Values.ingest-webhook.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.ingest-webhook.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.ingest-webhook.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.ingest-webhook.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.ingest-webhook.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.ingest-webhook.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
