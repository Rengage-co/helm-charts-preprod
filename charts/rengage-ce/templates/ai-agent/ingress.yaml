{{- if .Values.ai-agent.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ printf "%s-ai-agent" (include "rengage-ce.fullname" .) }}
  labels:
    {{- include "rengage-ce.labels" . | nindent 4 }}
  {{- with .Values.ai-agent.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ai-agent.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ai-agent.ingress.tls }}
  tls:
    {{- range .Values.ai-agent.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ai-agent.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ printf "%s-ai-agent" (include "rengage-ce.fullname" $) }}
                port:
                  number: {{ $.Values.ai-agent.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
