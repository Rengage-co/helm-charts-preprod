{{- if .Values.ai-agent.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ printf "%s-ai-agent" (include "rengage-ce.fullname" .) }}
  labels:
    {{- include "rengage-ce.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ printf "%s-ai-agent" (include "rengage-ce.fullname" .) }}
  minReplicas: {{ .Values.ai-agent.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.ai-agent.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.ai-agent.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.ai-agent.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.ai-agent.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.ai-agent.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
