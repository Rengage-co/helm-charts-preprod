{{- if .Values.presentation-layer.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ printf "%s-presentation-layer" (include "rengage-ce.fullname" .) }}
  labels:
    {{- include "rengage-ce.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ printf "%s-presentation-layer" (include "rengage-ce.fullname" .) }}
  minReplicas: {{ .Values.presentation-layer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.presentation-layer.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.presentation-layer.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.presentation-layer.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.presentation-layer.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.presentation-layer.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
