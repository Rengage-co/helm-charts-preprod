{{- if .Values.presentationlayer.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ printf "%s-presentationlayer" (include "rengage-ce.fullname" .) }}
  labels:
    app.kubernetes.io/name: {{ printf "%s-presentationlayer" (include "rengage-ce.name" .) }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- include "rengage-ce.labels" . | nindent 4 }}
  {{- with .Values.presentationlayer.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.presentationlayer.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.presentationlayer.ingress.tls }}
  tls:
    {{- range .Values.presentationlayer.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.presentationlayer.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ printf "%s-presentationlayer" (include "rengage-ce.fullname" $) }}
                port:
                  number: {{ $.Values.presentationlayer.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
