apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-conductor-ui" (include "rengage-ce.fullname" .) }}
  labels:
    {{- include "rengage-ce.labels" . | nindent 4 }}
spec:
  type: {{ .Values.conductor-ui.service.type }}
  ports:
    - port: {{ .Values.conductor-ui.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rengage-ce.selectorLabels" . | nindent 4 }}
