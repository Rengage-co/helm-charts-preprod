{{- if .Values.conductor-consumer.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ printf "%s-conductor-consumer" (include "rengage-ce.fullname" .) }}
  labels:
    {{- include "rengage-ce.labels" . | nindent 4 }}
  {{- with .Values.conductor-consumer.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.conductor-consumer.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.conductor-consumer.ingress.tls }}
  tls:
    {{- range .Values.conductor-consumer.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.conductor-consumer.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ printf "%s-conductor-consumer" (include "rengage-ce.fullname" $) }}
                port:
                  number: {{ $.Values.conductor-consumer.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
