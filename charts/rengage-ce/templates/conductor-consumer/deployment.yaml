apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-conductor-consumer" (include "rengage-ce.fullname" .) }}
  labels:
    {{- include "rengage-ce.labels" . | nindent 4 }}
spec:
  {{- if not .Values.conductor-consumer.autoscaling.enabled }}
  replicas: {{ .Values.conductor-consumer.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "rengage-ce.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.conductor-consumer.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "rengage-ce.labels" . | nindent 8 }}
        {{- with .Values.conductor-consumer.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.conductor-consumer.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "rengage-ce.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.conductor-consumer.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.conductor-consumer.securityContext | nindent 12 }}
          image: "{{ .Values.conductor-consumer.image.repository }}:{{ .Values.conductor-consumer.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.conductor-consumer.image.pullPolicy }}
          env:
            {{- toYaml .Values.conductor-consumer.env | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.conductor-consumer.service.port }}
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.conductor-consumer.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.conductor-consumer.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.conductor-consumer.resources | nindent 12 }}
          {{- with .Values.conductor-consumer.volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.conductor-consumer.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
