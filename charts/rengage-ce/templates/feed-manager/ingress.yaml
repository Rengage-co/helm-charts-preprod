{{- if .Values.feed-manager.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ printf "%s-feed-manager" (include "rengage-ce.fullname" .) }}
  labels:
    {{- include "rengage-ce.labels" . | nindent 4 }}
  {{- with .Values.feed-manager.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.feed-manager.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.feed-manager.ingress.tls }}
  tls:
    {{- range .Values.feed-manager.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.feed-manager.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ printf "%s-feed-manager" (include "rengage-ce.fullname" $) }}
                port:
                  number: {{ $.Values.feed-manager.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
