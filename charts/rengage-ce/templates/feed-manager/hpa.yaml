{{- if .Values.feed-manager.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ printf "%s-feed-manager" (include "rengage-ce.fullname" .) }}
  labels:
    {{- include "rengage-ce.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ printf "%s-feed-manager" (include "rengage-ce.fullname" .) }}
  minReplicas: {{ .Values.feed-manager.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.feed-manager.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.feed-manager.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.feed-manager.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.feed-manager.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.feed-manager.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
