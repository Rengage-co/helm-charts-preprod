{{- if .Values.data-retention.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ printf "%s-data-retention" (include "rengage-ce.fullname" .) }}
  labels:
    {{- include "rengage-ce.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ printf "%s-data-retention" (include "rengage-ce.fullname" .) }}
  minReplicas: {{ .Values.data-retention.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.data-retention.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.data-retention.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.data-retention.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.data-retention.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.data-retention.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
