{{- if .Values.data-retention.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ printf "%s-data-retention" (include "rengage-ce.fullname" .) }}
  labels:
    {{- include "rengage-ce.labels" . | nindent 4 }}
  {{- with .Values.data-retention.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.data-retention.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.data-retention.ingress.tls }}
  tls:
    {{- range .Values.data-retention.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.data-retention.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ printf "%s-data-retention" (include "rengage-ce.fullname" $) }}
                port:
                  number: {{ $.Values.data-retention.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
